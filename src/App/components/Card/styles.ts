import themes from '../themes'

export const container = {
    display:'flex',
    alignItems: 'center',
    backgroundColor: 'white',
    borderRadius: '10px',
    padding: '12px',
    boxShadow: themes.shadow,
}

export const containerIcon = {
    borderRadius:'50px',
    border: `1px solid ${themes.colors.grayColor3}`,
    padding: '12px'
}

export const containerText = {
    marginLeft:'20px',
}